// Stub firebase options file. The FlutterFire CLI (`flutterfire configure`) will
// generate a proper `firebase_options.dart` here with `DefaultFirebaseOptions`.
//
// Keep this stub so the app compiles before you run `flutterfire configure`.

import 'package:firebase_core/firebase_core.dart';

class DefaultFirebaseOptions {
  // When you run `flutterfire configure`, this will be replaced with an
  // implementation returning a `FirebaseOptions` appropriate for the
  // current platform.
  static FirebaseOptions get currentPlatform => throw UnimplementedError(
    'DefaultFirebaseOptions not configured. Run `flutterfire configure` to generate this file.',
  );
}
